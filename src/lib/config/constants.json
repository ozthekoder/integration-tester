{
  "validation": {
    "designator": "$",
    "types": {
      "$id": "string",
      "$log": "string",
      "$name": "string",
      "$before": "array",
      "$plugin": "string",
      "$op": "string",
      "$ops": "array",
      "$args": "array",
      "$payload": {
        "$expect": {
          "$value": "number|string|array|object",
          "$assert": "string",
          "$log": "string"
        },
        "$save": "number|string|array|object",
        "$waitFor": {
          "$value": "number|string|object",
          "$log": "string"
        }
      },
      "$timeout": "number",
      "$after": "array"
    },
    "keys": {
      "must": [
        "$id",
        "$before",
        "$after",
        "$timeout"
      ],
      "either_or": [
        ["$op", "$ops"],
        ["$args", "$ops"],
        ["$plugin", "$ops"],
        ["$payload", "$ops"],
        ["$timeout", "$ops"]
      ],
      "defaults": {
        "$before": [],
        "$after": [],
        "$ops": [],
        "$args": [],
        "$timeout": 10000
      },
      "can": [
        "$id",
        "$log",
        "$name",
        "$plugin",
        "$op",
        "$ops",
        "$args",
        "$payload",
        "$before",
        "$after",
        "$timeout"
      ],
      "requires": [
        [
          "$timeout",
          "$op",
          "$args",
          "$payload",
          "$plugin"
        ]
      ],
      "children": {
        "$payload": {
          "can": [
            "$expect",
            "$save",
            "$waitFor"
          ],
          "any": [
            "$expect",
            "$save",
            "$waitFor"
          ],
          "children": {
            "$expect": {
              "leaf": {
                "must": [
                  "$value",
                  "$assert"
                ],
                "can": [
                  "$value",
                  "$assert",
                  "$log"
                ]
              }
            },
            "$waitFor": {
              "leaf": {
                "must": [
                  "$value"
                ],
                "can": [
                  "$value",
                  "$log"
                ]
              }
            }
          }
        }
      }
    },
    "recursive": [
      "$before",
      "$after",
      "$ops"
    ]
  }
}
